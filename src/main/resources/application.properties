server.address=127.0.0.1
server.port=8090
server.context-path=/application

#FIXMESs:
#FIXME: Need to fix CustomBasicAuthenticationEntryPointSecurityConfig so that it actually works
#FIXME: Find a better solution for the Tomcat container for the jdbc driver (at the moment it works only if registering the dirver with Class.forName("com.mysql.jdbc.Driver") -> not nice)


# available spring profiles:
# - DEFAULT_AUTH_PROVIDER - use the default authentication manager in the DefaultAuthenticationProviderSecurityConfig class
# - CUSTOM_AUTH_PROVIDER - use the custom authentication manager in the CustomAuthenticationProviderSecurityConfig class
# - CUSTOM_SECURITY_FILTER - use a custom securityFilter
# - CUSTOM_BASIC_AUTHENTICATION_ENTRY_POINT - use a BasicAuthenticationFilter with CustomBasicAuthenticationEntryPoint
spring.profiles.active=CUSTOM_SECURITY_FILTER

spring.session.store-type=jdbc
spring.datasource.url=jdbc:mysql://localhost:3306/application_security_db
spring.datasource.username=root
spring.datasource.password=

application.security.unrestrictedUrls=/description, /security/login, /security/logout, /security/accessDenied
application.security.loginUrl=/security/login
application.security.logoutUrl=/security/logout
application.security.redirectToUrlAfterLogout=/login
logging.level.org.springframework.security=debug
logging.level.com.mysql=off